import React from "react";

const RightSide = () => {
  return (
    <React.Fragment>
      <div>
        Comments on that Tweet and if that comment has a comment then onClick of
        show Comments comments must load
      </div>
    </React.Fragment>
  );
};

export default RightSide;
